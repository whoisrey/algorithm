/*

  문제 설명은 README를 참고하세요.

 */

export default function imageArea(v) {
  const unMarkedArray = v.map((arr) => arr.map((arr) => (arr = false)));

  const directions = [
    [-1, 0],
    [1, 0],
    [0, -1],
    [0, 1],
  ];
}
